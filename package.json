{
  "name": "openrift-data",
  "version": "0.0.0",
  "private": true,
  "description": "Community-contributed card data for OpenRift",
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate-contributions.mjs",
    "check-uniqueness": "node scripts/check-uniqueness.mjs",
    "check": "bun run validate && bun run check-uniqueness",
    "build": "node scripts/build-community.mjs"
  },
  "devDependencies": {
    "ajv": "8.20.0"
  }
}
