{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/openriftapp/openrift-data/blob/main/community.schema.json",
  "title": "OpenRift Community Card Data",
  "description": "Schema for the built community.json release artifact. Generated from contributions/ by scripts/build-community.mjs. Mirrors openrift.app's admin candidate upload payload.",
  "type": "object",
  "required": ["provider", "candidates"],
  "additionalProperties": false,
  "properties": {
    "provider": { "const": "community" },
    "candidates": {
      "type": "array",
      "minItems": 0,
      "items": { "$ref": "card.schema.json" }
    }
  }
}
